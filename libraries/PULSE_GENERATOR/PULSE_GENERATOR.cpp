#include<PULSE_GENERATOR.h>

pulse_gen::pulse_gen(int number)
{
    switch(number)
    {
        case 0:
            enable_addr;
            year_h_addr;
            year_l_addr;
            usr_month_addr;
            usr_day_addr;
            usr_minutes_addr;
            usr_seconds_addr;
            usr width_high_3_addr;
            usr width_high_2_addr;
            usr width_high_1_addr;
            usr width_high_0_addr;
            usr width_period_3_addr;
            usr width_period_2_addr;
            usr width_period_1_addr;
            usr width_period_0_addr;

        case 1:
        case 2:
        case 3:
    }

}
